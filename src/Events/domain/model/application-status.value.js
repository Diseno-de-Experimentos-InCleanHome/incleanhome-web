/**
 * Value Object ApplicationStatus.
 * Capa: Events / domain / model
 */
export const ApplicationStatus = Object.freeze({
  PENDING: "pending",
  ACCEPTED: "accepted",
  REJECTED: "rejected",
  WITHDRAWN: "withdrawn",
});
